2	def( )+\w+\(.*\)( )*:
2	while (.+):
2	from [\w\.]+ import (\w+|\*)
2	class( )*\w+(\(( )*\w+( )*\))?( )*:
2	if( )+(.+)( )*:
2	elif( )+(.+)( )*:
2	else:
2	for (\w+|\(?\w+,( )*\w+\)?) in (.+):
1	\w+( )*=( )*\w+(\n|$)
1	print((( )*\(.+\))|( )+.+)
-1	(&{2}|\|{2})